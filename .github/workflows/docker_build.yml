name: Build and Store Docker Image

on:
  push:
    branches:
      - main # Change this to the branch you want to trigger the action

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}

    - name: Get current date
      id: current_date
      run: echo "::set-output name=date::$(date +'%Y-%m-%d')"

    - name: Build and push Docker image
      env:
        BUILD_DATE: ${{ steps.current_date.outputs.date }}
        BUILD_NUMBER: ${{ github.run_number }}
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: |
          your-dockerhub-username/repository-name:build-${{ env.BUILD_NUMBER }}
          your-dockerhub-username/repository-name:${{ env.BUILD_DATE }}
          
    - name: Build and push Docker image
      env:
        BUILD_DATE: ${{ steps.current_date.outputs.date }}
        BUILD_NUMBER: ${{ github.run_number }}
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: |
          ${{ secrets.DOCKER_HUB_USERNAME }}/repository-name:build-${{ env.BUILD_NUMBER }}
          ${{ secrets.DOCKER_HUB_USERNAME }}/repository-name:${{ env.BUILD_DATE }}